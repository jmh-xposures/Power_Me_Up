<div>
  <div>
  <p><% @pending_rentals.each do |rental| %>
    <%= rental.user.full_name %>
    <%= rental.user.email %>
    <%= rental.power.name %>
    <%= rental.power.short_description %>
    <%= rental.comment %>
      <p><%= link_to 'Accept', accepted_path(rental) %></p>
      <p><%= link_to 'Decline', declined_path(rental) %></p>
    <% end %>
  </p></div>
</div>




<p><%= link_to 'back to your profil', profile_path %></p>